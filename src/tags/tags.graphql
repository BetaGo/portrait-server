type Query {
  getTags: [Tag]
  tag(id: ID!): Tag
}

type Mutation {
  createTag(createTagInput: CreateTagInput): Tag
}

type Tag {
  id: Int
  name: String
}

input CreateTagInput {
  name: String!
}
